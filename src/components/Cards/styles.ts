import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        position: 'relative',

        marginTop: 30,
        marginBottom: 50,
        marginLeft: 24,
        marginRight: 24
    },
})
